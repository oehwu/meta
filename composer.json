{
    "name": "oehwu/meta",
    "description": "Package with various small handy classes in OEHWU projects",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Thomas Rieschl",
            "email": "thomas.rieschl@oeh-wu.at"
        }
    ],
    "require": {
        "php": ">=5.4",
        "ext-curl": "*"
    },
    "require-dev": {
        "infection/infection": "^0.8.1",
        "maglnet/composer-require-checker": "^0.2.0",
        "phpstan/phpstan": "^0.9.2",
        "phpunit/phpunit": "^7.0"
    },
    "autoload": {
        "psr-4": {
            "OEHWU\\Meta\\": "src"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "OEHWUTest\\Meta\\": "tests"
        }
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "check-deps": "vendor/bin/composer-require-checker",
        "infection": "vendor/bin/infection --only-covered --min-covered-msi=97",
        "phpstan": "vendor/bin/phpstan analyse src/ --level 7",
        "phpunit": "vendor/bin/phpunit",
        "tests": [
            "@check-deps",
            "@infection",
            "@phpstan",
            "@phpunit"
        ]
    }
}
